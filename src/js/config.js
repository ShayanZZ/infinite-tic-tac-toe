/**
 * Configuration for Infinite Tic-Tac-Toe
 * This file provides Supabase credentials for local testing without build tools
 * 
 * NOTE: In production, use environment variables instead
 */

// You can replace these with your credentials for local testing ONLY
// NEVER commit real credentials to your repository
window.SUPABASE_URL = "your_supabase_url";
window.SUPABASE_KEY = "your_supabase_anon_key";

// If you're using a local server like Vite that processes .env files,
// you don't need to modify this file. Environment variables will be used automatically. 