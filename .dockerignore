# This .dockerignore is intentionally at repo root because the build context
# is the repository root when running:
#   docker build -f docker/Dockerfile .
# Docker always reads .dockerignore from the build context root.

# Exclude local node modules, logs, debug data
node_modules
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-lock.yaml

# Exclude VCS and local tooling metadata
.git
.gitignore
.qodo
.vercel
.vscode

# Exclude environment files so they are not baked into the image
.env
.env.*
!.env.example

# OS / editor junk
.DS_Store
*.swp
